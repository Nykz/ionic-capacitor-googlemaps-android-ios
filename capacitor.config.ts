import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.capgmaps',
  appName: 'capacitor-google-maps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
